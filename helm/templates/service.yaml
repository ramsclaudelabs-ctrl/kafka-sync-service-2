apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}
  labels:
    app: contacts-service
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080
      name: http
  selector:
    app: contacts-service
